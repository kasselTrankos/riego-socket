const {tagged} = require('daggy');
const {empty, map} = require('fantasy-land');
const Riego  = require('./riego');
Riegos[of] = value => Riegos(value);
const Riegos = tagged('Riegos', {
  Some: [Riego],
  None: [],
});


